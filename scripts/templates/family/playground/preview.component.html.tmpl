<!--
  TODO: wire this up against the real Angular Material host element this
  directive decorates (e.g. `<button mat-button __SELECTOR_ATTR__ ...>`) —
  see icon-button-preview.component.html for the reference shape. This div is
  a structurally-valid placeholder only.
-->
<div class="preview-row">
  <div __SELECTOR_ATTR__ [size]="size()" [shape]="shape()" [toggle]="toggle()">__TITLE__</div>
</div>
