@use './configs' as configs;
@use 'sass:list';

$__KEBAB__-config: list.join(
  configs.$default-config,
  list.join(
    configs.$xs-config,
    list.join(
      configs.$s-config,
      list.join(configs.$m-config, list.join(configs.$l-config, configs.$xl-config))
    )
  )
);
