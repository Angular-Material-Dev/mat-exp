@use '../tokens' as tokens;

$config: (
  (
    // Default (fallback, unsized) entry — mirrors the 's' size until you
    // decide it needs to differ. See all-buttons/button/configs/_default.scss.
    tokens: tokens.$s-tokens,
  ),
);
