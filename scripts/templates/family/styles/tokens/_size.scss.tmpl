@use './common' as common-tokens;

// TODO: transcribe M3 Expressive spec values for the '__SIZE__' size of
// __KEBAB__ (m3.material.io → component → specs). Merge shared fragments in
// with `map.merge(common-tokens.$__KEBAB__-common-tokens, (...))` — see
// all-buttons/button/tokens/_xs.scss for the reference shape.
$tokens: common-tokens.$__KEBAB__-common-tokens;
